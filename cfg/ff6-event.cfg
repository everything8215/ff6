memory {
    dummy: start = 0, size = $10000 type = rw, file = "";
    bank_ca: start = $ca0000, size = $40000, type = ro, fill = no, fillval = $ff;
}

segments {
    event_triggers: load = dummy, type = ro;
    npc_prop: load = dummy, type = ro;
    map_init_event: load = dummy, type = ro;
    event_script: load = bank_ca, type = ro, align = $010000;
}
